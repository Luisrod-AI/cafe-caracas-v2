diff --git a/dist/client/R2ClientUploadHandler.js b/dist/client/R2ClientUploadHandler.js
index 2a732ef84a062b5358da0c367571c73307c8b56b..6c1f34ef13f81920cf84ea7ba127ae4c7e675af5 100644
--- a/dist/client/R2ClientUploadHandler.js
+++ b/dist/client/R2ClientUploadHandler.js
@@ -2,7 +2,7 @@
 import { createClientUploadHandler, getFileKey } from '@payloadcms/plugin-cloud-storage/client';
 import { formatAdminURL } from 'payload/shared';
 export const R2ClientUploadHandler = createClientUploadHandler({
-    handler: async ({ apiRoute, collectionSlug, docPrefix, extra: { chunkSize = 5 * 1024 * 1024 }, file, prefix, serverHandlerPath, serverURL, updateFilename })=>{
+    handler: async ({ apiRoute, collectionSlug, docPrefix, extra: { chunkSize = 5 * 1024 * 1024 } = {}, file, prefix, serverHandlerPath, serverURL, updateFilename })=>{
         const { sanitizedDocPrefix } = getFileKey({
             collectionPrefix: prefix,
             docPrefix,
@@ -19,9 +19,13 @@ export const R2ClientUploadHandler = createClientUploadHandler({
             path: serverHandlerPath,
             serverURL
         });
-        const endpoint = `${baseURL}?${String(new URLSearchParams(params))}`;
+        // Built per request, NOT once. `params` is mutated below to carry
+        // multipartId / multipartKey / multipartNumber, and URLSearchParams
+        // snapshots the object it is given — a single precomputed string would
+        // send the create-upload query on every request.
+        const endpoint = ()=>`${baseURL}?${String(new URLSearchParams(params))}`;
         // upload the file directly to R2 using the signed URL
-        const multipart = await fetch(endpoint, {
+        const multipart = await fetch(endpoint(), {
             method: 'POST'
         });
         if (!multipart.ok) {
@@ -44,7 +48,7 @@ export const R2ClientUploadHandler = createClientUploadHandler({
                 'Content-Length': String(body.size),
                 'Content-Type': 'application/octet-stream'
             };
-            const uploaded = await fetch(endpoint, {
+            const uploaded = await fetch(endpoint(), {
                 body,
                 headers,
                 method: 'POST'
@@ -59,7 +63,7 @@ export const R2ClientUploadHandler = createClientUploadHandler({
                 const headers = {
                     'Content-Type': 'application/json'
                 };
-                const complete = await fetch(endpoint, {
+                const complete = await fetch(endpoint(), {
                     body,
                     headers,
                     method: 'POST'
